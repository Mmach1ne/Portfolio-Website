import MuiLinkedInIcon from '@mui/icons-material/LinkedIn';
import type { SvgIconProps } from '@mui/material/SvgIcon';

export type LinkedInIconProps = SvgIconProps;

export function LinkedInIcon(props: LinkedInIconProps) {
  return <MuiLinkedInIcon {...props} />;
}
